MODEL-INDUSTRY-NEUTRAL INSTRUCTIONS
Read this file before making edits.

1) Snapshot and scope
- The attached files are a read-only snapshot for AI-assisted editing guidance.
- The AI must not change your local environment, Git settings, or any other repository.
- This repository contains an ordinary static site design for educational use.

2) Factual authority
- `MY_WEBSITE_FACTS.txt` is the only factual source for public content.
- If a field is marked unknown, the AI must keep it unknown in output.
- Do not invent names, schedules, addresses, contacts, links, affiliations, outcomes, testimonials, or statistics.

3) Mandatory demo constraints
- Keep all demo-safety states unless the owner explicitly requests conversion after full verification:
  - `noindex, nofollow`
  - robots blocking
  - clear fictional notice on every page
  - `data-demo-state="fictional"` in body
- Do not remove or hide these markers.

4) Working structure to preserve
- Do not remove the complete file-and-page structure: `site/index.html`, `site/meetings.html`, `site/about.html`,
  `site/newcomers.html`, `site/resources.html`, `site/connect.html`, `site/404.html`.
- Preserve shared shell classes and page files unless a request explicitly changes design/system.
- Preserve:
  - shared navigation behavior,
  - footer/forest structure,
  - keyboard behaviors,
  - skip link,
  - focus-visible styles,
  - no-JS usability.

5) Output requirements
- Return complete replacement files, not patch snippets.
- Do not return only diffs, fragments, ellipses, or directions like "replace this section".
- Preserve original filenames unless the request explicitly asks for different names.

6) Required report format after generation
The AI must state:
- what changed,
- what was preserved,
- which files changed,
- what the owner should inspect,
- any open uncertainty,
- what to verify before publishing.

7) Multi-page workflow requirement
- Keep all pages requested by the adopter, and preserve unchanged navigation and shared assets unless explicitly requested.
- If the request is for a single-page update, update only requested pages while preserving the rest of the site shell.

8) Publication boundary
- Publishing remains a human decision.
- The owner must open and approve resulting files before deployment.
