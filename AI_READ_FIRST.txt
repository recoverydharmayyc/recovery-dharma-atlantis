MODEL-INDEPENDENT INSTRUCTIONS — READ BEFORE EDITING

The attached project is a read-only snapshot. You cannot directly change the owner’s computer, Git account, hosting account, or live website. Publication remains a human decision.

1. Inspect the complete project before editing. Preserve its React, TypeScript, Vite, React Router, route-table, shared-component, content-module, meeting-domain, and layered-style boundaries unless the owner explicitly requests an architectural change.
2. MY_WEBSITE_FACTS.txt is the factual authority. Unknown information must remain unknown. Never invent names, dates, schedules, locations, addresses, contact details, links, affiliations, prices, policies, accessibility claims, testimonials, statistics, outcomes, or medical claims.
3. Put factual community wording in src/content/ where appropriate. Recurring local meetings are structured in src/content/meetings.ts. They are validated and interpreted by src/meetings/localMeetings.ts; do not replace that domain logic for a factual edit.
4. Temporary meetings and ordinary scheduled notices use src/content/announcements.ts and the existing timing system. Preserve explicit start and expiry times, safe internal links, malformed-data rejection, and automatic expiry.
5. Before visual work, read DESIGN_GUIDE.md. Presentation is owned by shared components and layered files under src/styles/. Change the community’s semantic palette, typography relationship, spacing, and surfaces through src/styles/tokens.css first. Preserve the “more interior space, less empty section space” law and viewport compositions. Do not turn the site into generic cards. A visual request should not rewrite meeting timing, sanitization, caching, or factual data. A factual request should not rewrite the design system.
6. Preserve routes, working navigation, normal document scrolling, responsive behavior, keyboard access, focus visibility, reduced-motion behavior, the real not-found route, and the safe application error boundary. Do not add route fades or a motion library for an ordinary content change.
7. Recovery Dharma Global meeting data is an attributed external public preview. Preserve its source disclosure, request timeout, omitted credentials, safe URL checks, record sanitization, bounded results, versioned short-lived cache, cached-failure state, and full-directory fallback unless the owner explicitly asks to remove the whole preview. Remote records are untrusted and must never be rendered as HTML.
8. Preserve fictional mode until the owner explicitly confirms every public fact is verified and expressly asks to convert the site. Do not remove noindex/nofollow, public/robots.txt blocking, the compact fictional label, the footer fictional notice, or the durable fictional setting.
9. Do not add a real contact endpoint, form, newsletter, analytics, backend, payment flow, environment-variable requirement, private integration, remote font, or unverified external dependency unless expressly requested and approved.
10. Return a complete clean source ZIP when supported. Never include node_modules, dist, PUBLISH-THIS-FOLDER, AI-COPY, .git, .env files, logs, screenshots, or editor caches. Otherwise return every complete changed file.
11. Never return only diffs, patches, fragments, ellipses, or line-number directions. Preserve filenames unless a new file was explicitly requested.
12. List every changed file, explain what changed and what was preserved, identify uncertainty, and state exactly what the owner must inspect. The owner must preview every route and approve the result before publication.
