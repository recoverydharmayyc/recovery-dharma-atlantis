MODEL-INDEPENDENT INSTRUCTIONS — READ BEFORE EDITING

The attached project is a read-only snapshot. You cannot directly change the owner’s computer, Git account, hosting account, or live website. Publication remains a human decision.

1. Inspect the whole project before editing. Preserve the React, TypeScript, Vite, React Router, shared-layout, page-component, and content-module architecture unless the owner explicitly asks for a redesign.
2. MY_WEBSITE_FACTS.txt is the factual authority. Unknown information must remain unknown. Never invent names, dates, schedules, locations, addresses, contact details, links, affiliations, prices, policies, accessibility claims, testimonials, statistics, outcomes, or medical claims.
3. Put factual community changes in src/content/ where appropriate. The recurring local meeting data is in src/content/meetings.ts. Temporary meetings and ordinary announcements use src/content/announcements.ts. Keep components responsible for presentation and behavior.
4. Preserve the existing design unless redesign was requested. Change visual identity through src/theme.css whenever possible instead of scattering colour changes through page styles.
5. Preserve routes, working navigation, responsive behavior, keyboard access, focus visibility, and the site-shell composition. Keep one route-transition owner; do not add a second page fade or entry animation.
6. Recovery Dharma Global meeting data is an external public source. Preserve its attribution, safe URL checks, record sanitization, short-lived cache, cached-failure state, and full-directory fallback unless the owner explicitly asks to remove the preview. Remote records are untrusted and must never be rendered as HTML.
7. Preserve fictional mode until the owner explicitly confirms every public fact is verified and expressly asks to convert from fictional demo to a real site. Do not remove noindex/nofollow, public/robots.txt blocking, the compact fictional label, the footer fictional notice, or the durable fictional demo state.
8. Do not add a real contact endpoint, form, newsletter, analytics, backend, payment flow, environment-variable requirement, private integration, or external dependency unless expressly requested and verified.
9. Return a complete clean source ZIP when the tool supports it. Never include node_modules, dist, PUBLISH-THIS-FOLDER, AI-COPY, .git, .env files, logs, or editor caches. Otherwise return every complete changed file.
10. Never return only diffs, patches, fragments, ellipses, or line-number instructions. Preserve filenames unless the owner asks for a new file.
11. After producing files, list every changed file; explain what changed and what was preserved; state exactly what the owner must inspect; and name any uncertainty. The owner must preview and approve the result before publication.
